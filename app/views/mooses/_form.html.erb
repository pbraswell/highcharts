<%= form_for(@moose) do |f| %>
  <% if @moose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@moose.errors.count, "error") %> prohibited this moose from being saved:</h2>

      <ul>
      <% @moose.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
